<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="searchViewStyle">@style/MySearchView</item>
    </style>

    <style name="MySearchView" parent="Widget.AppCompat.SearchView">
        <item name="searchHintIcon">@null</item>
    </style>

    <style name="BtnStyle">
        <item name="android:height">50dp</item>
        <item name="android:layout_width">80dp</item>
        <item name="android:padding">10dp</item>
    </style>

    <!-- dayTheme -->
    <style name="dayTheme" parent="AppTheme">
        <item name="android:textColor">#000000</item>
        <item name="android:background">#FFCCCC</item>
    </style>

    <!-- nightTheme -->
    <style name="nightTheme" parent="AppTheme">
        <item name="android:textColor">#000000</item>
        <item name="android:background">#CCFFFF</item>
    </style>

    <style name="PlayerSeekBarStyle">
        <item name="android:maxHeight">2dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/player_seekbar_play_progress</item>
        <item name="android:minHeight">2dp</item>
        <item name="android:thumb">@drawable/player_slider_p</item>
    </style>

</resources>
