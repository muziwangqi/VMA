<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <LinearLayout
        android:id="@+id/id_ll_main"
        android:layout_width="match_parent"
        android:layout_height="50dp" >

        <!-- add -->

        <ImageButton
            android:id="@+id/id_ib_add"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="center_vertical"
            android:src="@drawable/add" />

        <!-- phone -->

        <TextView
            android:id="@+id/id_tv_phone"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:text="phone"
            android:textSize="16sp" />

        <!-- music -->

        <TextView
            android:id="@+id/id_tv_music"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:text="music"
            android:textSize="16sp" />

        <!-- setting -->

        <TextView
            android:id="@+id/id_tv_setting"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:text="setting"
            android:textSize="16sp" />

        <!-- search -->

        <ImageButton
            android:id="@+id/id_ib_search"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_gravity="center_vertical"
            android:src="@drawable/search" />
    </LinearLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/id_vp_scoll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_below="@id/id_ll_main" >
    </android.support.v4.view.ViewPager>

</RelativeLayout>